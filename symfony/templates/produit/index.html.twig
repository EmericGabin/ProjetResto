{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}
    <h1>Produit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prix</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            {% if restaurent.id == produit.restaurent.id %}
            
                <tr>
                    <td>{{ produit.id }}</td>
                    <td>{{ produit.nom }}</td>
                    <td>{{ produit.prix }}</td>
                    <td>{{ produit.description }}</td>
                    {% if is_granted('ROLE_ADMIN') %}
                        <td>
                            <a href="{{ path('produit_show', {'id': produit.id}) }}">show</a>
                            <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>
                        </td>
                    {% else %}
                        <td>
                            <a href="{{ path('produit_show', {'id': produit.id}) }}">show</a>
                            
                        </td>
                    {% endif %}
                </tr>
            {% endif %}
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('restaurent_show', {'id': restaurent.id}) }}">back to restaurant</a>
{% endblock %}
