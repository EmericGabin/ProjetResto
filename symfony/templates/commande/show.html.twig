{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>PrixTotal</th>
                <td>{{ commande.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('commande_index') }}">back to list</a>
    <a class="btn btn-primary" href="{{ path('ligne_commande_index', {'id': commande.id}) }}">ligne(s) de la commande</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
